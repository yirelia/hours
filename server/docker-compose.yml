version: '3.8'

services:
  server:
    build:
      context: ..
      dockerfile: server/Dockerfile
    container_name: hours-server
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./logs:/app/logs
